<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.PixelMind" parent="Theme.AppCompat.NoActionBar">
        <item name="android:statusBarColor">#080810</item>
        <item name="android:navigationBarColor">#080810</item>
        <item name="android:windowBackground">#080810</item>
    </style>
</resources>
```

Then also open `app/build.gradle.kts` → click ✏️ edit → find this line:
```
implementation("androidx.compose.material3:material3")
```

And add this line directly below it:
```
implementation("androidx.appcompat:appcompat:1.6.1")
